#include "scene.h"
#include "collision.h"

const cen::type_id_t Collider::_tid = cen::TypeIdGenerator::getInstance().getNextId();

const cen::type_id_t CollisionObject2D::_tid = cen::TypeIdGenerator::getInstance().getNextId();